<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.book_your_nails">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/submark"
        android:label="@string/BookYourNails"
        android:roundIcon="@drawable/submark"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookYourNails"
        tools:targetApi="31">
        <activity
            android:name="com.example.bookyournails.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>